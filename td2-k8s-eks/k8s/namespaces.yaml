# TD 2 - etape 2 : les deux namespaces de la plateforme
# Squelette a completer - les TODO-2.x renvoient a l'etape 2 du README.
# Avant tout apply : grep -rn "a-completer" k8s/ ne doit plus rien retourner.
# Validation : kubectl apply --dry-run=client -f k8s/namespaces.yaml
apiVersion: v1
kind: Namespace
metadata:
  # TODO-2.1 : le nom du namespace des applications (voir docs/architecture.md)
  name: a-completer
  labels:
    app.kubernetes.io/part-of: semishop
    # TODO-2.2 : ajoutez un label environment avec la valeur poc
---
apiVersion: v1
kind: Namespace
metadata:
  # TODO-2.3 : le nom du namespace de la supervision
  name: a-completer
  labels:
    app.kubernetes.io/part-of: semishop
    # TODO-2.4 : le meme label environment que ci-dessus
